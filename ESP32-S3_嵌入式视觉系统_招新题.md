# 电工电子科技创新中心招新题
## ESP32-S3 嵌入式视觉系统

## 一、题目说明

本题使用 **ESP32-S3 集成 CAM 开发板** 完成一个简单的嵌入式视觉系统。

开发板需要通过摄像头获取图像，并将实时画面传输至电脑端显示。在此基础上完成指定的视觉识别任务。

本题允许使用 **AI、网络资料、官方文档及开源代码** 辅助完成。

评测不仅关注最终功能是否实现，同时关注程序的：

- 处理帧率
- 识别准确性
- 识别稳定性

电脑端图像传输的延迟不作为评价指标。

---

# 二、硬件

统一使用指定的：

- ESP32-S3 CAM 开发板，8 MB PSRAM，Flash ≥ 8 MB

---

# 三、公共要求

无论完成基础题还是提高题，均需要实现：

1. ESP32-S3 能够正常获取摄像头图像。
2. 电脑能够实时查看摄像头画面。
3. ESP32-S3 在采集图像的同时完成相应视觉处理任务。
4. 电脑端能够实时显示视觉处理结果。
5. 程序应能够持续运行，而非仅对单张图片进行处理。

---

# 四、基础题：指定色块视觉定位

## 4.1 任务目标

在规定的摄像头画面分辨率下，识别画面中的 **指定颜色色块**，并计算该色块的位置。

基础题统一使用：

**320 × 240**

作为视觉处理画面分辨率。

---

## 4.2 色块识别

程序需要从摄像头画面中识别指定颜色的目标色块。

当目标存在时，需要计算目标色块的 **几何中心坐标**：

```text
Target Center: (x, y)
```

坐标系规定：

- 图像左上角为原点 `(0, 0)`
- x 轴正方向向右
- y 轴正方向向下

对于 320 × 240 的画面，画面中心为：

```text
(160, 120)
```

---

## 4.3 中心连线

在电脑端显示的实时画面中，需要标出：

- 画面中心
- 色块几何中心
- 两点之间的连线

示意：

```text
┌──────────────────────────────┐
│                              │
│                   █████      │
│                   ██●██      │
│                 / █████      │
│               /              │
│             ×                │
│                              │
└──────────────────────────────┘

×：画面中心
●：色块几何中心
```

目标移动时，上述信息应实时更新。

---

## 4.4 像素位置误差

实时计算色块几何中心相对于画面中心在 x、y 两个方向上的像素差。

定义：

```text
dx = target_x - center_x
dy = target_y - center_y
```

在本题规定分辨率下：

```text
dx = target_x - 160
dy = target_y - 120
```

例如：

```text
Target Center: (203, 91)

dx = +43 px
dy = -29 px
```

这些数据需要能够在电脑端实时查看。

---

# 五、提高题：指定人员人脸识别

提高题在已经能够完成摄像头实时图像获取与电脑端显示的基础上，进一步完成人脸检测与人脸识别。

---

## 5.1 多人脸检测

程序需要检测画面中所有 **足够大、能够正常进行识别的人脸**。

对于检测到的每一张有效人脸，需要：

- 找到人脸位置
- 在电脑端实时画面中使用矩形框框出人脸

例如：

```text
┌────────────────────────────────┐
│                                │
│   ┌────────┐      ┌────────┐   │
│   │        │      │        │   │
│   │  Face  │      │  Face  │   │
│   │        │      │        │   │
│   └────────┘      └────────┘   │
│                                │
└────────────────────────────────┘
```

画面中同时存在多张有效人脸时，应尽可能检测并框出全部人脸。

---

## 5.2 指定人脸识别

提前录入一个 **指定人员** 的人脸信息。

程序在检测画面中所有有效人脸的同时，需要判断其中是否存在该指定人员。

对于检测到的人脸，需要至少能够区分：

```text
TARGET
```

与

```text
UNKNOWN
```

例如：

```text
┌──────────────────────────────────┐
│                                  │
│   ┌──────────┐    ┌──────────┐   │
│   │ UNKNOWN  │    │  TARGET  │   │
│   │          │    │          │   │
│   └──────────┘    └──────────┘   │
│                                  │
└──────────────────────────────────┘
```

如果指定人员出现在多人画面中，应能够正确找到并标识该人员。

如果指定人员没有出现，则不应将其他人员错误识别为目标人员。

---

# 六、性能与效果要求

基础题与提高题均将以下指标作为重要评测内容。

## 6.1 帧率

程序需要能够持续处理摄像头画面。

需要关注视觉算法实际完成处理的帧率，而不仅仅是电脑端视频显示的刷新速度。

---

## 6.2 准确性

### 基础题

主要考察：

- 是否能够正确识别指定颜色色块
- 色块几何中心计算是否准确
- `dx`、`dy` 计算是否正确

### 提高题

主要考察：

- 是否能够正确检测人脸
- 多人情况下是否能够正确框出有效人脸
- 是否能够正确识别指定人员
- 是否能够正确将其他人员判断为非指定人员

---

## 6.3 稳定性

当目标基本保持静止时，程序输出结果应尽可能稳定。

基础题中应避免色块中心坐标出现明显无规律跳动。

例如相对稳定的输出：

```text
(160, 120)
(161, 120)
(160, 121)
(159, 120)
(160, 119)
```

提高题中应避免同一人员在短时间内频繁出现类似：

```text
TARGET
UNKNOWN
TARGET
UNKNOWN
TARGET
```

的识别结果跳变。

---

# 七、评测重点

本题重点考察：

- 功能完成情况
- 视觉识别准确性
- 视觉识别稳定性
- 实际处理帧率
- 面对陌生问题时的分析与解决能力
- 对所使用方法和程序的理解程度

允许使用 AI 辅助完成，但需要能够理解自己的实现，并能够解释程序的主要工作流程及关键设计。
