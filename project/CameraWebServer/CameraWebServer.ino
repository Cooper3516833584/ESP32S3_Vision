#include "camera_base.h"

// ================================================================
// 主程序
// 摄像头、Wi-Fi AP 和视频推流已经封装在 camera_base.cpp 中。
// 视觉算法可通过 cameraBaseSetFrameProcessor() 接入推流取得的每一帧。
// 保持本文件不变时，烧录后就是可直接使用的 320 x 240 视频基座。
// ================================================================

void setup() {
  // 必须保留：启动摄像头和视频流基座。
  cameraBaseBegin();

  // ---------------- 初始化代码写在下面 ----------------

}

void loop() {
  // 必须保留：让 ESP32 后台网络任务及时运行；该函数不会长时间阻塞。
  cameraBaseUpdate();

  // ---------------- 循环代码写在下面 ------------------

}
